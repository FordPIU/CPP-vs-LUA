{
    "version": "2.0.0",
    "tasks": [
    {
      "label": "codeBuild",
      "type": "shell",
      "command": "cmake --build .",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "options": {
        "cwd": "${workspaceFolder}/build"
      },
      "problemMatcher": [
        "$msCompile"
      ]
    }
  ]
}